# QXWeb (Vite) — copy to web.toml in repo root (gitignored).

api_base_url = "http://localhost:3000/api/v1"
log_level = "debug"
# Public site origin for SEO (canonical URLs, Open Graph, robots.txt, sitemap.xml):
# site_url = "https://mc.qx-dev.ru"
# Prod Docker build: VITE_API_BASE_URL=https://mc.qx-dev.ru/api/v1 (see infra/docker/.env.prod.example)
# Prod: launcher is bundled at /downloads/qx-launcher.exe (override only for external CDN):
# launcher_download_url = "/downloads/qx-launcher.exe"
