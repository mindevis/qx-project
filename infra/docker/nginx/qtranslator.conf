# Placeholder — replaced by qTranslator deploy (/opt/qtranslator).
# QX up.sh preserves a real vhost (contains qtranslator-api) across QX releases.
# nginx requires this file to exist for the compose volume mount.

server {
    listen 80;
    server_name qt.qx-dev.ru;
    return 503;
}
